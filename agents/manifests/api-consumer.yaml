agent:
  name: api-consumer
  layer: 1
  trigger: "consumir APIs, fetch, axios, tanstack-query, graphql, rest, datos desde
    backend, SWR, RTK Query"
  description: "Consumidor de APIs desde el frontend. Maneja fetching, cachÃ©, optimistic
    updates, GraphQL, REST, y patrones de integraciÃ³n con backend."

mcp_bindings:
  - context7
sub_agents:
  - frontend-api-integration
  - shared-api-integration

instructions:
  persona: persona.md
  workflow: workflow.md
