agent:
  name: api-consumer
  layer: 1
  trigger: "consumir APIs, fetch, axios, tanstack-query, graphql, rest, datos desde backend, SWR, RTK Query"
  description: "Consumidor de APIs desde el frontend. Maneja fetching, caché, optimistic updates, GraphQL, REST, y patrones de integración con backend."

mcp_bindings:
  - filesystem
  - context7

sub_agents:
  - api-integration
  - graphql

instructions:
  persona: persona.md
  workflow: workflow.md
  patterns: patterns.md
