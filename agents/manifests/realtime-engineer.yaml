agent:
  name: realtime-engineer
  layer: 1
  trigger: "websockets, SSE, server-sent events, polling, tiempo real, datos en vivo, socket.io, rxjs"
  description: "Especialista en datos en tiempo real. Websockets, SSE, polling, y patrones de comunicación bidireccional entre frontend y backend."

mcp_bindings:
  - filesystem
  - puppeteer

sub_agents:
  - realtime

instructions:
  persona: persona.md
  workflow: workflow.md
