agent:
  name: sdd-init
  layer: 1
  trigger: "bootstrap SDD context and project configuration"
  description: "Bootstrap SDD context and project configuration"

mcp_bindings:
  - filesystem

sub_agents: []

instructions:
  persona: persona.md
  workflow: workflow.md
